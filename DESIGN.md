# 設計書 — 仮題『詠唱順 / ORDER』

ターン制デッキ構築ローグライト。
**Slay the Spire の枠組みに、Noita の杖(呪文実行機械)を移植する。**

一行で言うと:

> **手札を並べて1つの呪文を組み立てる。順序が全て。**

---

## 1. 設計の前提

Noita の魅力は二層に分かれている。

1. **物理シミュレーション** — 流体・爆発・材質。空間の上に成立している
2. **杖の実行機械(VM)** — 詠唱順、修飾のバッファリング、多重詠唱、起動の入れ子、マナ、リチャージ

**本作は 2 のみを移植し、1 は完全に捨てる。**

理由: 1 を再現しようとすると工数の大半が「Noita に勝てない劣化物理」に消える。
一方 2 は物理と無関係な純粋な仮想機械であり、STS の「山札→手札→捨札」パイプラインと構造が一致する。

### 空間の扱い

空間は捨てるが、**STS には敵の列がある**。
貫通・分裂・跳弾・波及・連鎖は、敵列の隣接関係だけで成立する。
実装コストはほぼゼロで、Noita の幾何がかなりの部分残る。

---

## 2. コアループ

STS ではカードを1枚プレイすると効果が出る。本作はここを変える。

> 手札からカードを **杖** に装填する。杖は順序付きの実行列であり、
> 詠唱すると全体が **1つの呪文** として解決される。

**1ターン = 引いたカードでその場の呪文を組み立てるパズル。**

### ターン構造

```
ドロー 5 / エナジー 3 / マナ 毎ターン +3 (上限 10)

■ 装填(Load)   手札のカードを杖の任意の位置に差し込む   コスト: エナジー 0〜1
■ 詠唱(Fire)   杖を左から実行する。使用カードは捨札へ   コスト: マナ
■ 通常プレイ    防御・ドロー・汎用カードは STS と同様

ターンをまたいでも杖の中身は残る = 複数ターンかけて充填できる
```

### 中心的な緊張: 時間

Noita の杖のリスクは「大きい杖はリチャージが長い」だった。
STS に移すと、**リスクは空間ではなく時間になる**。

大呪文を組む数ターンのあいだ、プレイヤーは殴られ続ける。
**今撃つか、まだ積むか。** これが毎ターンの中心的な判断。

> Noita の杖が「空間の設計」だったなら、本作は「時間の設計」。

---

## 3. 実行モデル(VM)

カード種別は **4つだけ**。増やすと破綻する。

| 種別 | 動作 | 例 |
|---|---|---|
| **発射体** | バッファされた修飾を全て消費して実体を撃つ | 火球 / 氷弾 / 雷矢 / 石礫 / 黒球 |
| **修飾** | バッファに積まれる。**単体では何もしない** | 三分裂 / 貫通 / 加速 / 跳弾 / 拡大 / 追尾 |
| **多重詠唱** | 続く N 枚をまとめて取り出し同時に解決 | 二重詠唱 / 散弾詠唱(5) |
| **起動** | 直後の発射体にペイロードを持たせる。着弾時、その敵の位置から続きのカードが再詠唱される(入れ子) | 着弾時起動 / 時限起動 |

- 杖は左から右へ、**全体が1回の詠唱で解決される**
  (Noita のような詠唱ごとの分割はしない。ターン制では読みにくくなるだけ)
- 発射体は構造体、修飾はその関数。**組み合わせを手で列挙し始めたら設計失敗**

```ts
type Projectile = {
  element:  Element      // 火 / 氷 / 雷 / 物理 / 闇
  damage:   number
  pierce:   number       // 敵列を貫通する数
  bounce:   number       // 跳弾回数
  split:    number       // 分裂数
  splash:   number       // 隣接への波及
  homing:   boolean
  payload:  Card[] | null  // 起動によるペイロード
}
```

### 例: 同じ4枚で結果が桁違いになる

手札 `塗油` `三分裂` `貫通` `火球` / 杖容量 4

**悪い順序**
```
[火球] [三分裂] [貫通] [塗油]
→ 素の火球1発。修飾は火球より後ろなので何も乗らない
```

**良い順序**
```
[塗油] [三分裂] [貫通] [火球]
→ 塗油   : 対象に「油」付与
→ 三分裂 : バッファに積む
→ 貫通   : バッファに積む
→ 火球   : 3発に分裂し、それぞれ敵列を貫通
→ 油の敵に着弾 → 爆発 → 隣接する敵へ延焼
```

**順序が唯一の解を持たず、盤面(敵の並び・状態)に依存すること。**
これが成立していれば設計は正しく、成立していなければ全部やり直し。

### 例: 入れ子

```
[起動] [火球] [二重詠唱] [氷弾]
→ 火球がペイロードを抱えて飛ぶ
→ 着弾した敵の位置から、氷弾 x2 が二次詠唱される
```

---

## 4. 自爆 — Noita の「自分の杖が一番危険」の移植

空間が無いので、以下2つで置き換える。**数値バランスではなくルールで抑える。**

### 不安定度
修飾と多重詠唱を積むほど杖の不安定度が上がる。
閾値を超えて詠唱すると **暴走** — 自傷 / 対象のランダム化 / カードの消滅。

### マナ超過
マナが足りない杖も撃てる。ただし**不足分を HP で支払う**。

> 「勝てる。ただし血を吐く」という選択肢が毎ターン卓上にある。

---

## 5. 属性反応

| 状態 | 反応 |
|---|---|
| 油 + 火 | 爆発。隣接する敵に延焼 |
| 濡れ + 雷 | 敵列全体に連鎖 |
| 凍結 + 打撃 | 粉砕。ダメージ倍 |
| 毒 + 火 | 毒霧爆発。列全体に毒 |
| 油 + 氷 | 固着。行動阻害 |

**注意: 属性反応それ自体は目新しくない**(原神をはじめ既出)。
本作の売りは **杖(順序)** であって反応表ではない。反応表は順序に意味を与えるための燃料。
この優先順位を間違えないこと。

---

## 6. STS 枠の流用

- **マップ・報酬・カード除去・エリート・ボス** — そのまま流用。ここを発明する必要はない
- **ポーション → 巻物**

### レリック = 杖の部品

「エナジー+1」のような足し算ではなく、**VM の規則そのものを書き換える**ものにする。

- 杖の容量 +1
- 先頭の修飾はマナ 0
- 詠唱後、修飾カードだけ杖に残る(毎ターン積み直さなくていい)
- 杖を右から実行する
- 不安定度が上限を超えても暴走しない代わりに HP を払う

### 敵 — 攻撃対象に「杖」を加える

| 敵 | 行動 |
|---|---|
| 消去者 | 杖からランダムに1枚を捨てさせる |
| 逆転者 | 杖の順序を反転する |
| 封印者 | 杖のスロットを1つ塞ぐ |
| 分解者 | 杖の中の修飾1枚を「不発」に変える |

STS の敵は HP と手札にしか触れない。
**「組み上げかけた機械を壊しに来る敵」は既存のデッキビルダーに無い攻撃面。** 本作の重要な差別化点。

---

## 7. 既知のリスクと対策

| リスク | 対策 |
|---|---|
| **複雑さ**。デッキビルダーが死ぬ原因の第一位 | **杖容量は 3 から始める。** 3枚なら有限のパズル。5〜6は終盤の解放要素。カード種別は4つで固定 |
| **プレビュー不在** | 装填するたびに「今撃つとこうなる」を完全表示。UIの飾りではなく、**遊べるかどうかの分水嶺**。ここをケチると即死 |
| **Noita は壊れることが魅力、STS は壊れないことが魅力**(相反する) | **壊れる方を採る。** 神の杖が組める瞬間が Noita の全て。**上限ではなく頻度で調整する** |
| **修飾を無限に積むのが最適解になる** | 不安定度とマナ超過(§4)がルールとして潰す |
| **順序に唯一の正解が生まれる** | 修飾は数値バフではなく**幾何と挙動**を変えるものに限定する(分裂・貫通・跳弾・波及・起動) |

---

## 8. 最小プロトタイプ

> **反省**: 当初この節に書いた範囲（属性反応・不安定度・多重詠唱・起動を含む）は
> MVP ではなかった。メタ進行を抜いただけの全部盛りで、検証したい問い 1 つに対して
> 8 系統が乗っていた。実際に組んだら複雑すぎて遊べなかったので、以下に縮小した。

### 検証したい問いは 1 つだけ

> **カードを並べる順序を考えるのは面白いか。**

これを測るのに必要なのは修飾と発射体だけ。他は答えが出た後に足す。

### v8 — 批判レビューを受けた修正

外部視点の批判レビューを 2 本（不具合／デザイン）走らせ、指摘を潰した。

#### 致命的だったもの

| 症状 | 原因 | 対処 |
|---|---|---|
| ボスを 4 回倒すと操作不能 | 根本強化が 4 種しかなく、報酬が 0 個になっていた | 繰り返し取れる強化を受け皿に追加 |
| 同じ修飾を 2 枚拾っても 1 枚ぶん | `Object.assign` が上書きしていた | 加算に変更 |
| 三分裂が単体相手だと威力半減の罠 | 飛び先が尽きると複製が消えていた | 生存する敵を巡回。単体でも 9 → 12 |
| 図鑑を開いたまま歩けて、裏で戦闘が始まる | keydown に図鑑の判定が無かった | 図鑑中は入力を止める |
| 射的画面を離れても物理が回る | 火花の分岐に画面判定が無かった | 画面判定を追加し、火花は物理を回さず減衰させる |
| 札が画面上へ吹き飛び空中で固まる | 天井が無く、打ち切りが速度 0 にしていた | 天井を追加。打ち切り時は落として回収 |

#### ピクセルの大きさが場所ごとに違っていた

同じ「1 ドット」が 6 種類の大きさで描かれ、canvas は縦横で拡大率が違った
（200x112 を 16:9 に引き伸ばしていた）。

- ステージ幅を **192 の倍数に吸着**させ、1 ゲーム単位 = 整数 px（`--u`）に
- 盤面を **192x108（16:9 ちょうど）** にし、canvas は表示と 1:1
- スプライトの箱はすべて `--u` の整数倍。札は 15u x 21u（5:7 を整数で保つ）
- 照準と散りの円をアンチエイリアスの `arc()` からドット打ちに

#### そのほか

- パレットを 24 色に整理（枠 14 色・紺 18 色・金 11 色に散っていた）
- 枠・影・文字サイズを各 1 系統に
- 敵スプライトを共通規則で描き直し（足の行を揃え・輪郭あり・目を必ず入れる）
- Silkscreen に和文グリフが無く、和文だけ別書体に落ちていた
- 戦闘・報酬にも射的屋と同じ板壁と棚・カウンターを敷いて場所を統一
- 詠唱の演出を Space / ボタンで早送りできるように（最大 6.3 秒あった）
- ダンジョンのタイルをクリックで移動できるように
- 拾えなかったラウンドが完全に無為だったので、空の杖で詠唱すると防御 +6

#### バランス

ボットで通しプレイを回して測ったところ、**当初は 40 回中 40 回が 1 階で死亡**した。

| 版 | 到達層の分布 |
|---|---|
| 修正前 | 1 階 40 |
| 敵を弱化 | 1 階 10 / 2 階 1 / 3 階 1 |
| 修飾の重ねがけ修正後 | 1 階 9 / 2 階 3 |
| 攻撃力低下・発射体増・戦闘後 +5 | 1 階 1 / 2 階 2 / 3 階 1 / 4 階 1 / 6 階 1 |

なお最初の測定は**ハーネス側の不具合**で無効だった。`fire()` の後に
`S.mode` を戻すのは requestAnimationFrame のループで、`page.evaluate` の
同期実行中は回らないため、ボットは 1 ラウンドに 1 発しか撃てていなかった。

### 未解決だった 3 点 — 決着（v9）

1. **入れ替え 1 回が、落下順を無意味にしている。** 432 ラウンドの測定で、
   順序が問題になった場面は「既に正しい 303 / 1 回の入れ替えで直る 129 / 直せない 0」。
   平均 2.5 枚の杖に対して隣接交換 1 回は完全なソートに等しい。
   救済として入れたものが主役になっている。
2. **狙いが順序を決める度合いが低い。** 狙った札が最初に落ちる確率は 37%
   （命中力強化で 46%、散弾では 24% に下がる）。
   「狙いが呪文の設計になる」という核心が、実測では成立していない。
3. 射的画面に敵の情報が無く、**呪文を組む時点で相手が見えない**。

### v9 — 3 つの未解決への回答と、全画面対応

#### 1. 入れ替えは「最初から持っている機能」をやめ、強化にした

救済として無条件に配っていたものが主役になっていたので、**初期値 0** にした。
落下順がそのまま杖の順序になる状態が既定で、入れ替えは以下でしか手に入らない。

| 出どころ | 種別 | 効果 |
|---|---|---|
| 手直し | ボス報酬（根本強化） | 入れ替え +1 |
| 器用な指 | 繰り返し取れる強化 | 入れ替え +1 |

これで「順序を狙いで作る」が既定、「順序を後から直す」が投資になる。
入れ替えを 2 回積んだビルドは実質 v8 と同じ強さになるが、それは
**そこに枠を使った結果**であって、無料で配られたものではない。
HUD は 0 のとき「入れ替え なし」と出す。

#### 2. 序盤の狙いにくさは、そのまま受け入れる

狙った札が最初に落ちる確率 37% は、**プールが 18 枚しかない序盤の話**である。
命中力強化を積み、外れ札をプールから増やさずに済めば、狙いは効くようになる。
ここを初期値から高くすると、強化の伸びしろを先食いする。
「最初はうまくいかない、伸ばすと効く」を成長の軸としてそのまま採用する。

ただし**強化の効きが分かる形**であることは条件なので、強化の説明文に
実際の増減を書き、誇張をやめた（下記 散弾の項）。

#### 3. 射的画面に敵の情報を出した

射的画面の上帯（通常は「射的」の看板）を、戦闘中は**敵の一覧**に差し替える。

- 敵ごとに 16x16 のスプライト・HP バー・`現在値/最大値`・`攻 N`
- 倒れた敵は灰色に落とす
- **クリックで狙う敵を先に選べる**（選択中は金の枠）

これで「相手の HP と攻撃力を見てから、どの札をどの順で落とすか決める」が
射的の時点で成立する。3 点目は素直に情報不足だったので、情報を足した。

#### 散弾を弱体化から戻した

一度「散りが広がる」罰を薄めたが、**ビルドによっては散弾が正解**という
指摘を受けて元に戻した。入れ替えが強化になった今、

- 狙って順序を作る型 → 命中力・単発威力を伸ばす
- 順序を運に任せて枚数で殴る型 → 散弾 + 入れ替えを伸ばす

の 2 系統が両立する。説明文は「弾 3 発。束をまとめて崩せるが、
散りが広がり狙った順に落ちにくい」と、**得と損の両方**を書く形にした。

#### 入れ替えを 0 にした効果（実測）

ボットで 14 回通しプレイし、詠唱 1415 回の杖の並びを記録した。

| 指標 | 値 |
|---|---|
| 修飾を含む詠唱 | 848 回 |
| **うち、修飾が発射体より後ろにあって無駄になった** | **395 回（47%）** |
| 平均の杖枚数 | 2.18 枚 |
| 到達層の分布 | 1 階 1 / 3 階 2 / 4 階 4 / 5 階 3 / 6 階以上 4 |
| 平均プール | 26.6 枚 |

v8 では入れ替え 1 回が実質「完全なソート」で、順序ミスは 0 だった。
0 にしたことで**約半分の詠唱が順序で損をする**状態になり、
「どの札をどの順で落とすか」が初めて実際の損得になった。
入れ替えを買うかどうかも、この 47% を買い取る取引として意味を持つ。

#### 全画面

`⛶` ボタン（枠の右上）または `F` キーで全画面。
全画面中は下の注記を隠し、ステージは画面の幅と高さの**両方**から
192x108 の整数倍を取り直す（上限を撤廃したので 1152px 以上にも伸びる）。

> 実装上の罠 1: 拡大率を親要素の `clientWidth` から求めていたが、
> 全画面用に足したラッパが flex で中身に合わせて縮む要素だったため、
> 「幅を決めるのに幅が要る」循環になって `--u` が 2px に潰れた。
> 全画面時は `window.innerWidth` から直接求めるようにして解消。
>
> 実装上の罠 2: **押しても全画面にならなかった。** artifact は iframe の中で
> 動くため、`allowfullscreen` が付いていないと `requestFullscreen()` は
> 拒否され、`document.fullscreenEnabled` も false になる。しかも
> 返る Promise を握り潰していたので、押しても無反応に見えていた。
> ネイティブ全画面が使えない場合は**擬似全画面**（`position:fixed` で
> 枠ごと覆う）に落とし、どの環境でも必ず拡大できるようにした。
> 戻すのは `F` または `Esc`。

### v10 — 見た目。「センスの問題」ではなく「規律の欠如」だった

作者から「UI がダサい。初心者インディーズゲーム感が拭えない」。
外部視点の批判レビューを 3 巡走らせたところ、原因は美的センスではなく
**宣言したルールを実装が守っていないこと**だと分かった。

CSS の先頭には「24 色。すべてここから引く。CSS に直接の色は書かない」と
書いてあったが、実ファイルには **107 個の 16 進色**が直書きされていた。
しかも ΔE 2 以下（人間の目には同じ）の紺が 6 組。塗り分けの意図が伝わらず、
「なんとなく濁った紺の面」になっていた。

#### 採った方向

作者が 3 案から選んだのは **Into the Breach / Loop Hero 的な硬派ドット**。
限定パレットを厳格に守り、1 ドットの大きさを完全に統一し、
装飾ではなく規律で見せる。

#### 決めた規律

| 規律 | 内容 |
|---|---|
| 色 | `:root` の **16 色**（`--c0`〜`--cf`）以外は CSS にも canvas にも書かない |
| 半透明 | 使わない。中間の明るさが要るところは市松のディザで出す |
| 1 ドット | `--u` は必ず偶数。その半分 `--px` が 1 ドット。枠・影・スプライトの箱・余白はすべて `--px` の整数倍 |
| ドットピッチ | 2 種類だけ。UI の中は `--px`、世界の中は `--px*2` |
| 文字 | `--t1`〜`--t4` の 4 段。すべて `--px` の整数倍。`em` の端数を使わない |
| 記号 | フォントの記号（⛶ ⇄ ⚔ ▼）を使わない。8x8 で自前で打つ |
| 無効表現 | `opacity` / `grayscale` を使わない。暗いパレットに差し替える |
| 輪郭 | スプライトの輪郭は必ず `--c0`。地に `--c0` を使わない |
| 枠 | 枠は「プレイヤーが触る UI」の記号。世界の中の存在（敵・景品）には使わない |
| 光源 | 上から 1 つ。影は 1 ドット・不透明・向き固定。ぼかし影は使わない |
| 金 | 主役の輪郭と、いま選べるものだけに使う |

#### 直した内訳

**1 巡目**（色数・ドット寸法・敵の箱・光源・縁日の記号）

- 107 色を Lab 空間の最近傍で 16 色に機械変換し、意図が壊れる 24 箇所を手で直した
- 半透明 12 箇所を全廃
- 枠の太さ 1px/2px/3px/4px の混在を `--px` 一本に
- 文字の `.5em`〜`1.8em` の小数倍（7.44px や 16.2px になっていた）を 4 段の整数 px に
- 敵から枠と背景を外して床に立たせ、接地影を敷いた。選択は足元の金の影＋頭上の 1 ドット三角
- 射的屋に紅白幕・提灯・木の棚を入れ、鉄砲を作り直した（画面で一番大きい物が、
  一番描き込みの少ないベタ矩形 4 個だった）
- ダンジョンの床タイルに汚れのドット 4 種と縁の段差

**2 巡目**（ルールを書いたのに守れていない値）

| 症状 | 原因 |
|---|---|
| 敵 3 体に輪郭が無い | palette[4]（＝輪郭）が `#151A2E` で、背景も `#151A2E` だった |
| 踏破済みの部屋アイコンが消える | `DEADPAL` の色が部屋タイルの地と同色だった |
| ドットを打った意味が消えた札 3 種 | 本体とハイライトに同じ値が入っていた（石礫・防御・蟲） |
| 同じ絵が画面ごとに違う大きさ | 1 ドットが 3px / 5px / 6px / 9px の 4 種類あった |
| 蟲と石人が同じ灰 | 16 色に灰は 1 つしかないのに 2 体に配っていた |

**3 巡目**（構図と端数）

- `em` の端数が 87 箇所残っていた。手札 5 枚の間隔が 6.6px で、
  札の左端が 1 枚ごとに 0.6px ずれていた
- 報酬の札を 2 倍で見せようと `--u` を局所上書きしたら、
  `--px: calc(var(--u)/2)` と循環参照になり絵の箱だけ縮んで壊れた

#### 動きを入れた

静止画が 7 枚あるだけで、ゲームの画面になっていなかった。
照準中は canvas が完全に静止しており、提灯は揺れず幕はなびかなかった。

| 入れたもの | 参考 |
|---|---|
| 着弾で 2 フレーム止める | Celeste が全ての衝突でやっている |
| 発砲で銃口が光り、画面が 1 ドット揺れる | Nuclear Throne |
| 敵の待機モーション（1 ドット 2 コマ・個体ごとに位相をずらす） | Darkest Dungeon の呼吸 |
| 被弾はパレット差し替えの白飛び 2 コマ | Shovel Knight |
| 提灯の明滅・幕の裾の揺れ | 止まっている画面は絵であってゲームではない |

#### 学んだこと

指摘の 8 割は「値の間違い」だった。ルールを決めること自体は難しくないが、
**決めたルールに違反した値が残っていないかを機械的に測る**仕組みが無いと、
宣言は看板にしかならない。いまは以下を数えられるようにしてある。

```
色数:  grep -oE '#[0-9A-Fa-f]{6}' | sort -u | wc -l   →  16
半透明: grep -c 'rgba('                                →  0
端数:  grep -oE '[0-9]*\.[0-9]+em' | wc -l            →  0
```

### v7 の範囲（現在の実装）— ダンジョンを歩く

```
ダンジョン  15 x 11 のタイル。中央の縦通路から横に枝が伸びる十字型
移動        矢印キー / WASD / スワイプで 1 マスずつ歩く。部屋の中心に乗ると発動
部屋        入口 / 戦闘 x2 / 宝箱 / 休息 / ボス（配置は毎回入れ替わる）
報酬        勝つと 3 択 1、引き直し 1 回
図鑑        開発用。実装済みの札・強化・敵・現在の状態を一覧できる
```

#### 歩く形にした理由

節点を選ぶ地図（v6）は「どちらの道を選ぶか」しか操作がなく、
ダンジョンを進んでいる感触がなかった。タイルの通路を歩く形にすると、
部屋に入るという行為が明示的になり、引き返す余地も出る。
壁は床に接する空きマスを自動で塗って作っている。

#### 戦闘の演出

詠唱の解決を一気に適用していたため、何が起きたか見えなかった。
**複製した盤面で解決手順だけを先に記録し、それを 1 手ずつ再生する**ようにした。

| 手順 | 表示 | 間隔 |
|---|---|---|
| 修飾がバッファに乗る | ログに `+ 三分裂` | 240ms |
| 発射体が撃たれる | ログに `▸ 火球 [分裂3 貫通2]` | 380ms |
| 命中 | 敵の上に `−5` が浮き、HP バーが減る | 340ms |
| 撃破 | 敵の上に「撃破」 | 520ms |
| 敵の反撃 | 体力の数値が赤く光る | 520ms |

浮かぶ数値は再描画で作り直されると演出がやり直しになるため、
描画対象から外した専用レイヤに積んで 1 秒後に自分で消える。

### v6 の範囲（現在の実装）— ダンジョン

```
ダンジョン  1 階 = 5 列の部屋。中列は 2 部屋に分岐し、どちらを通るか選ぶ
部屋        入口 / 戦闘 / 宝箱 / 休息 / ボス
戦闘        射的（コルク 2 発）→ 詠唱 → 敵の反撃、を敵が全滅するまで繰り返す
報酬        勝つと 3 択 1。引き直しが 1 回
            通常戦闘: 札の追加 か 鉄砲の強化
            宝箱:     札 3 枚から 1 枚
            ボス:     根本的な強化。取ると次の階へ
休息        体力 +26
```

#### 報酬でプールが育つ

`POOL` は定数をやめ、`S.pool`（実行時の配列）にした。報酬で選んだ札が
そのままプールに積まれ、次のラウンドから棚に並ぶ。
**撃ち落とす対象そのものを育てる**のがこのゲームのデッキ構築にあたる。

鉄砲の強化（命中力・散弾・威力・バズーカ）は取得済みを除外して提示する。

#### 根本的な強化（ボス報酬）

| 名前 | 効果 |
|---|---|
| 弾薬箱 | コルク +1（1 ラウンドの射撃回数） |
| 長い杖 | 杖の容量 +1 |
| 手直し | 詠唱前の入れ替え +1 |
| 厚い外套 | 最大体力 +25 して全回復 |

容量・コルク数・入れ替え回数も定数をやめ、実行時の状態にした
（`S.cap` / `S.corkMax` / `S.swapMax`）。

### v4 の範囲（現在の実装）— 射的

エナジーを廃止。カードは手札から引くのではなく、**射的で撃ち落として得る**。

```
射的台    3 棚 × 2〜3 個の景品（プールから抽選）。コルクは 1 ラウンドに 2 発
獲得      撃ち落とした景品がカードになる。落ちた順がそのまま杖の順序
救済      詠唱前に隣り合う 2 枚を 1 回だけ入れ替え可
カード    7 種（発射体 3 / 修飾 3 / 防御 1）。プール 17 枚
杖        容量 5
通貨      なし。制約はコルクの数と腕前だけ
ラウンド  射的 → 詠唱 → 敵の行動
```

### 核心：落下順 ＝ 詠唱順

射的が「カードを N 枚もらう装置」なら、それは手の込んだガチャで、
並べ替えパズルとは無関係のままになる。そうしないための一点:

> **景品を倒して次の景品にぶつける。ドミノの連鎖が、そのまま呪文のプログラムになる。**

`三分裂` を撃って `火球` に倒し込めば 三分裂 → 火球 の順で落ち、修飾が正しく乗る。
狙いが雑で火球だけ先に落ちれば修飾は不発。**射的の腕前と呪文の設計が同じ操作になる。**

### 射的の仕組み（v5）

弾道の弧をやめ、**レティクルで直接狙う**方式にした。低い角度が死んでいた問題も同時に消える。

```
狙い    レティクルを合わせてクリック。着弾は点線の円内にばらける
散り    円の半径 = 命中力。強化で縮む
景品    棚に 2〜3 の山。各山は 1〜2 段の積み重ね
獲得    落ちた順がそのまま杖の順序
```

#### 剛体演算

「当たったら落ちる」はスイッチであって物理ではない。押されるだけ・隣にぶつかる・
傾いて耐える・倒れて落ちる、が計算されないと射的の手触りにならない。
そこで **2D 剛体ソルバを自前で実装**した。

- OBB 同士の分離軸法（SAT）で法線・侵入量・接触点を求める
- 逐次インパルス法（反発 0.02 / 摩擦 0.55 / 10 反復）＋ 位置補正
- 回転あり。**着弾点に力積**を与えるので、当たり所で倒れるか滑るかが変わる

Matter.js を使う案もあったが、このサンドボックスから cdnjs に到達できず
**一度も動かさないまま公開することになる**ため見送った。自前なら検証できる。

##### 棚の奥行きの扱い

横から見た 2D 画面には「棚の奥行き」がない。そのため倒れた札が
幅の広い棚の上に寝るだけで落ちなかった（実測: 命中しても落下は 20〜28%）。
**傾きが 0.95 rad を超えた札は棚との衝突判定から外す**ことで、
手前に落ちる挙動にした。倒れるかどうかの判定自体は力積と摩擦の物理計算のまま。

##### 検証

| 項目 | 結果 |
|---|---|
| 静止安定性（3 秒放置） | 崩落 0 枚・最大ずれ 0.15・最大傾き 0.006 |
| 600 ラウンド連続 | NaN 0 件・飛散 0 件 |
| 1 ラウンド（コルク 2 発）の獲得 | 平均 2.58 枚 / 収穫ゼロ 7% |
| 分布 | 1 枚 21% / 2 枚 22% / 3 枚 23% / 4 枚 17% / 5 枚 10% |
| 当てたが落ちなかった | 12〜32%（当たり所による） |

力積は 120 から 1200 まで振って調整し、現在は **800**。小さいと動くだけで落ちず、
大きいと束ごと吹き飛ぶ。表示上の速さは物理ステップ数（6 → 3/フレーム）で別に落としてある。
1 ラウンド（コルク 2 発）で平均 1.98 枚、収穫ゼロ 14%。

##### 代償

**結果が台本どおりにならなくなった。** 以前は「下段を撃てば必ず下 → 上の 2 枚」
という学習可能な規則だったが、いまは物理が決める。
狙いと結果の対応は弱まり、代わりに手触りと再現性のある因果が入った。
どちらが良いかは遊んでみないと分からない。

#### 強化軸

鉄砲の性能を最初からパラメータとして持たせてある（`BASE_GUN`）。
第 1 層クリア後に 3 択で 1 つ選ぶ。

| 強化 | 効果 |
|---|---|
| 銃身の削り出し | 散り 5 → 2.5（命中力） |
| 散弾に詰め替え | 弾 1 → 3 発・散り 11・威力 0.7 倍 |
| 火薬増し | 威力 1.6 倍（連鎖が伸びる） |
| 筒を太く | 着弾点の周囲 14 も巻き込む（バズーカ） |

### 見た目

**全画面をドット絵で統一**した。

- **カードは 1 つの部品 `cardHTML()` を両画面で使い回す。**
  射的の棚に並ぶ札も、戦闘の手札も、同じ HTML・同じ CSS・同じ 5:7。
  射的側だけ canvas で描いていた頃は必ず見た目がずれたので、
  棚の札も DOM 要素にし、剛体の座標と角度を CSS transform に流している。
  絵は `CARDS[].art` に後から差し替えられる（null の間は仮のアイコン）
- **敵とカードのアイコンは実際のドット絵**。1 文字 = 1 ピクセルの文字列で
  スプライトを持ち、`spriteSVG()` が 1 ピクセルずつ矩形に展開する
  （`shape-rendering:crispEdges` で整数座標に乗るので本物のドット）。
  細い線の SVG は安っぽく見えたため全廃した
- 背景・棚・電球・鉄砲・照準は低解像度（600×336）の canvas に描いてピクセル拡大
- 配色は射的も戦闘も同じ暗い藍。唯一の暖色は照準の朱で、詠唱ボタンの橙と対応する

### 操作

射撃は**ボタン**（Space）。画面クリックと矢印キーは照準の移動だけに使う。
撃つ瞬間を明示的にしたのと、後で撃ち方が増えたときにボタンを増やせるため。

#### 3D にしない理由

剛体物理で本当に倒すと、同じ場所に当てても毎回違う倒れ方をする。
**狙って倒す再現性**がこのゲームの核心なので、見た目だけ立体にして
当たり判定は 2D のまま保っている。

### 次に足すべきもの

成長軸は**プール**にある。景品の追加・除去・強化、棚の数、コルクの弾数、
コルクの威力（＝連鎖の長さ）。ここが将来のデッキ構築に相当する。


### 保留中（削除ではない。核が面白いと分かってから足す）

| 保留するもの | 理由 |
|---|---|
| マナ / 不安定度 / 暴走 / マナ超過 | 通貨が 5 本あった。v4 では通貨自体を廃止した |
| 多重詠唱 / 起動・ペイロード | 入れ子が 2 種類同時にあった。容量 5 では使い切れない |
| 属性反応と状態（油・濡れ・凍結） | 杖の順序とは別の第 2 の系統。他ジャンルで既出でもあり売りにならない |
| 杖を攻撃する敵（消去者・分解者） | 第 3 の系統。差別化点として有望だが、土台の後 |
| 跳弾・追尾・増幅 | 修飾の効果軸が 6 本あった。3 本に絞った |
| 1 ラウンド 2 詠唱 | v3 で入れたが、射的が判断の中心になったので一旦戻した |
| レリック、マップ、メタ進行、ショップ | 元から範囲外 |

### 実装の履歴

| 版 | 内容 | commit |
|---|---|---|
| v1 | 全部盛り。8 系統・通貨 5 本。複雑すぎて遊べなかった | `e759c7c` |
| v2 | 発射体と修飾だけに縮小。今度は「攻撃か防御か」しかなく浅い | `1bd1f86` |
| v3 | 1 ラウンド 2 詠唱、横画面の一枚絵 UI | `3914bbb` |
| v4 | エナジー廃止。射的でカードを獲得。落下順 ＝ 詠唱順 | 現在 |

---

## 付録: 検討して捨てた案

### A. 仮名ワードクラフト型（『書架』）
図書館を舞台に、仮名タイルを組んで単語を綴る。しりとりが連鎖、`ん` が必殺技、
既読ペナルティがメタ進行。テーマとメカニクスの一致は美しかったが、
**「呪文を集めて並べ替える」方向ではなかった**ため却下。

### B. グリッド + セルオートマトン型
8x8 のグリッド上で弾を実体として飛ばし、油・水・火・氷・毒霧・瓦礫が
セルオートマトンで伝播する。Noita の物理を離散化して創発エンジンにする案。
**工数の 8 割が「Noita に勝てない劣化物理」に消える**ため却下。
この判断が §1 の前提になっている。
