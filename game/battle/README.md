# battle — 戦闘画面（案F「手札」）／簡易プレイ

## ファイルの構成

`app.html` と `index.html` は<b>生成物</b>。直接編集しない。

| | |
|---|---|
| `src/b_*.txt` | 中身。この順につないで 1 つの `<script>` にする |
| `cards/*.png` | 札の絵。**ファイル名がそのまま札の id**。決めごとは `cards/README.md` |
| `wheel.png` | 盤の下地（回っていないとき用に焼いたもの） |
| `build.py` | 上を集めて `app.html` を作る |
| `app.html` / `index.html` | 生成物 |

札を足すときは `cards/` に PNG を置いて、`src/b_game.txt` の `DECK` に 1 行足して
`./build.sh`。絵だけでは札にならない（属性・効果・区画の数が要る）。

## 遊べるところ（簡易）

**盤は実際に回る。**`wheel/` の物理と絵をそのまま持ってきてある。

### 手番の流れ

```
自分の手番のはじめ   火傷を受ける（数値ぶんのダメージ／そのあと 1 減る）
盤を回す（1 手番に 2 回）
                       玉をまとめて投げる → 収まった区画の札が全部手札に入る
                       → 引いた順に 1 枚ずつ、光らせながら効かせる
自分の手番おわり     敵の盾を消す
敵の手番のはじめ     敵が火傷を受ける
敵が動く             攻撃／盾
敵の手番おわり       自分の盾を消す → 自分の手番へ
```

**玉はまとめて投げる。**1 回転で玉の数だけ札が出る。
それを**1 手番に 2 回**（レバーをクリック、または Space / Enter）。

| | いま | |
|---|---|---|
| 玉の数 | 1 | 1 回転で投げる玉。強化で増える想定 |
| 回せる数 | 2 | 1 手番に回せる回数 |

どちらも持ち物（テスト用）から変えられる。

### 見せ方の間

一瞬で数字が変わると、何が起きたのか読めない。
**「札が光る → 数字が飛ぶ → 消える」の 3 拍**に分けて、それぞれ間を置く。

| | |
|---|---|
| 出た札の読み上げ | 1100 ms |
| 効いている札が光る | 640 ms（その札だけ持ち上げて真鍮の縁を付ける） |
| 数字が頭の上に出る | 1000 ms |
| 消えてから次の札まで | 240 ms |
| 敵の 1 手 | 520 ms |
| 火傷 | 900 ms |

数字は**当たった相手の頭の上**に出す。画面の隅の文字だけだと、
誰に何が起きたのか結び付かない。字は 16 ドット（書体の設計サイズの 2 倍）。

実況の行は**敵の HP の下（床の上）**に置く。筐体の上に置くと、
いま光っている札とぶつかる。

### 状態

| | |
|---|---|
| 火傷 | 手番のはじめに数値ぶんのダメージ。同時に 1 減る（だんだん和らぐ）。**盾では防げない**（体の中で燃えているものなので） |
| 力 | 与える攻撃に数値ぶん足す |
| 盾 | ダメージを肩代わりする。守り切れないぶんは HP が削れる。**貼った側の「次の相手の攻撃が終わる」まで**残り、そこで消える |

HP の帯の左に、盾（青）・火傷（赤）・力（真鍮）の順で記章が積む。
数字だけにせず、色と位置と数字の 3 つで出す。

### 札 5 枚（絵は送ってもらったもの）

| | 属性 | 効果 | 区画 |
|---|---|---|---|
| 錆びたボロ剣 | 無 | 最前の敵に 5 ダメージ | 5 |
| 錆びたクナイ | 無 | 全ての敵に 3 ダメージ | 4 |
| 錆びたハンマー | 無 | 最前の敵のシールドを破る／最前の敵に 8 ダメージ | 3 |
| ボロ松明 | 火 | 最前の敵に炎上 3 | 3 |
| ボロい火矢 | 火 | 最後の敵に炎上 3 | 2 |

**確率は区画の数で決める。**「この札を出やすくする」＝「区画を増やす」。
同じ札が隣り合わないように配ってある（機械投擲は初期条件が近いので、
固めると隣どうしに固まって出る）。

杖（引いた順が呪文になる）の要素は無い。札は 1 枚ずつ独立して効く。

### 手札が増えたとき

置ける範囲（`x 236..636`）からはみ出さないように間隔を詰める。
それでも詰めすぎると名前も効果も読めないので、**出すのは新しい 4 枚まで**。
古いぶんは左に「＋n」で出す。いちばん新しい 1 枚だけ持ち上げて、
いま引いたのがどれか分かるようにしてある。

## 区画の色は属性で決める

盤は 168 ドットしかないので、区画に字は入らない。**色だけが手がかり**になる。
だから色を 1 枚ずつ勝手に選ばず、**属性ごとの色の帯**から取る。

| 属性 | 色 |
|---|---|
| 無（ただの武器） | 茶。同じ属性の中では明るさを変えて見分ける |
| 火 | 赤〜橙 |
| 氷 | 青灰 |
| 毒 | 緑 |

札を足すときは `el` を書くだけでよい。同じ属性の中で上から順に色が配られる。
茶は**明るさの差がいちばん大きくなる順**に並べてある。隣どうしの段
（`#dda06a` と `#c8874f`）を続けて配ると、盤の上では見分けられない。

色は盤の色表（PAL）に入っている値だけを使う。表に無い色は丸められて、
選んだ色と出ている色が食い違う。

### 鉢の木を 1 段暗くした

茶の区画を置いたら、**鉢の木と同じ明るさで区画が溶けた**。
鉢の輪を全部 1 段暗くして、「区画がいちばん明るい」という関係を作った。
属性の色を増やすときも、この関係だけは崩さない。

## 持ち物（テスト用の組み替え）

右上の「持ち物」を押すと、**デッキと玉の数をその場で変えられる**。

- 札ごとに区画を 0〜20 で増減。合計は 2〜46（玉が入る幾何の上限）
- 出る確率がその場で出る
- 玉は 1〜6
- 触ると盤の色分けも即座に変わる

本番では買い物・報酬でしか変えられなくなる予定。いまはテスト用。

### 拡大したら、絵も本当に細かくする

ただの `scale(2)` だと 168 ドットの絵が太くなるだけで、**細かさは増えない**。
拡大する意味が「大きく見える」だけになる。

そこで<b>寄せ切ったあとに裏画面ごと 336 ドットで焼き直す</b>。
縮尺も枠も裏画面の幅 `BW` から出しているので、`DOTS_W` を変えるだけで全部ついてくる。
物理はメートルで解いているので、絵の解像度を変えても出目は変わらない。

| | 裏画面 | 表示 |
|---|---|---|
| ふだん | 168×140 | 等倍 |
| 拡大中 | 336×278 | 等倍 |

動きは低解像度のまま `scale(2)` で見せて、**止まってから高解像度に差し替える**。
拡大しながら焼き直すと、動いている最中に絵が飛ぶ。

倍率は整数だけ。1.5 倍などにすると画素が 2px と 3px に割れる。
枠は canvas と**同じ入れ物**に入れてある。別々に置くと、拡大したときに
枠だけ元の大きさで残る。

### 画面を 3 層に分けた

| | |
|---|---|
| `#bg` | 動かないもの（壁・床・筐体）。1 回だけ組む |
| `#whl` | 盤の枠と canvas。**ここだけは組み直さない** |
| `#fg` | 変わるもの（人物・手札・操作・HUD）。手番ごとに組み直す |

ぜんぶ 1 つの文字列で組み直すと、canvas が毎回作り直されて盤が消える。

### 名前がぶつかった 2 か所

盤の物理を持ってきたときに、同じ名前が別の意味で使われていた。

- `S` … ドット絵の色の指定 `S(ramp, step)` ／ 盤の縮尺。盤側を `SCL` にした
- `G` … 重力加速度 9.81 ／ ゲームの状態。ゲーム側を `GS` にした

どちらも `Identifier has already been declared` で即死したので気づけた。
黙って上書きされる名前だったら見つけられなかった。

## 並び

```
 y   0 +------------------------------------------+
       | 世界 -- 天井・石の壁・松明・木の床        |  自分と敵が
       |         自分 左 / 敵 右 / 右上に所持金    |  1 本の線に立つ
   168 +------------------------------------------+
       | 筐体 -- 盤の窓 左 / 手札を扇に持つ 右     |
   360 +------------------------------------------+
```

案E から変えたところ。

| | |
|---|---|
| 盤を小さく | 300×250 → **192×160**。世界が 100 → **168 ドット**に増えた。案E の一番の問題（世界が帯になる）はこれで消えた |
| 出た札 → 手札 | 溝に立てるのをやめ、**扇に開いて持つ**。溝に立てると「機械が吐いた物」に見える。持つと「自分の手」になる |
| 人物を大きく | 倍率 1.5 → **2**。世界が 68% 広がったのに人物がそのままだと、構図が下に偏る |
| 松明 | 広がった壁を埋めるのに模様を足すのではなく、**光源そのもの**を置いた。凹凸を全部「左上から」の光で作っているので、松明も左に寄せる。<br>**光の粒（ディザの丸い光）は入れていない。**暗い壁の上だと黄色い点がただの汚れに見える |
| 見出しを削除 | 「出た札」の見出しを捨てた。札が扇で並んでいれば手札だと分かる |

### 傾けた札はやめた

一度は角度ごとに絵を焼いて扇に開いていたが、**札に名前と効果を載せた時点で成り立たなくなった**。

- 文字を絵に焼き込むと 8 ドットに潰れて読めない（DotGothic16 は 16px 設計）
- だから文字は札の上に HTML で重ねる
- 重ねる以上、札は立てておくしかない（傾けると文字まで傾く）

代わりに真ん中を少し持ち上げた弧にして、「持っている」形だけ残した。

## 見つけた間違い

### 文字が箱からずれていた

置いた箱に `font-size` を書いていなかったので、箱が body の 14px を継いで
**行の高さが 17px になり**、中の 8px の字がその行の基準線まで押し下げられて、
箱の下にはみ出していた。

見た目は「文字だけ位置がおかしい」なのに、原因は**箱のほう**にある。
flex で並べた所だけ正しく見えていたのは、flex だと行の基準線を使わないため。
`#scr` に `font-size:8px; line-height:10px` を入れて、根元から直した。

検算に「置いた箱から出ている文字が無いか」を足した。

### 1. ディザが 1 つも効いていなかった

`dither()` が返す値は `url("data:image/svg+xml;...")` だった。
これは `style="..."` の中に入る。**`url("` のところで style 属性が閉じてしまい、
`background-image` が丸ごと消えていた。**

エラーは何も出ない。壁の階調・床の階調・機械の明かり・筐体の肌——
**ディザで作ると README に書いたものが、ぜんぶ 1 つも描かれていなかった。**
松明の光が出ないので調べて、初めて気づいた。

引用符をシングルにして直した（`encodeURIComponent` は `'` を encode しないが、
この SVG の中に `'` は無い。属性は全部ダブル＝`%22` になる）。

直した直後は**全部が濃すぎた**。見えないまま数字を決めていたので当然で、
壁・床・筐体・光の密度を全部付け直した。
検算（`bshot.js`）に「ディザが本当に効いているか」を足した。

### 2. 人物の足元が FOOT より下にずれていた

`actor()` が枠に `SHAPES[sp].w/h`（**倍率を掛ける前**の大きさ）を使っていた。
絵は倍率を掛けた大きさで描かれるので、枠から右下へはみ出す。
倍率 1.5 で 12 ドット、2 で 24 ドット下にずれていた。HP の帯が体に重なる。

`spriteW / spriteH` から取るように直した。
**描画と配置に位置を別々に計算させない。**同じ間違いを何度もしている。

## いちばん大事な決めごと（案B から変わらない）

**画面ぜんぶを仮想 640×360 のドットで組んで、最後に整数倍で拡大する。**

盤の裏画面が 300×250 なので、仮想画面ではそれが等倍で置ける。
こうすると盤のドット・人物のドット・枠線・文字が**ぜんぶ同じ大きさの升目に乗る**。

## 札の規格

| | 寸法 | 使う所 |
|---|---|---|
| 大 | 220×308 | 読ませる所（デッキ・図鑑・買い物） |
| 中 | 110×154 | 盤面に持つ手札 |
| 小 | 45×63 | 一覧・持ち物 |

比はどれも 7:5（縦長）。場面が変わっても変わらない。

**大は中のちょうど 2 倍。**絵を 220×308 で 1 枚描けば、中は 2 で割るだけで格子が壊れない。
枠は大きさごとに描き直す（縮小すると細部が潰れて、ただの汚い模様になる）。
ルーン帯と費用の丸は「大」だけ。

### 絵を差し替えるとき

`cardEl(x, y, t, d)` に 3 通りで渡せる。

| | 中 | 大 |
|---|---|---|
| 札まるごとの絵（いま使っているのはこれ） | 110×154 | 220×308 |
| 名前の帯（絵の中にある。ここに文字を重ねる） | x 6..102, y 11..21 | ×2 |
| `d.art` / `d.glyph` | コードで起こす仮の中身 | 同左 |

**この寸法ちょうどか、その整数倍**で描いて渡すこと。
半端な大きさを縮めると、せっかく揃えた格子が壊れる。

## 素人くさく見える原因として潰したもの

### 格子

| | |
|---|---|
| 格子の混在 | 仮想 640×360 の 1 つの升目に統一 |
| 半端な拡大 | **整数倍だけ**。1.5 倍だとドットが 2px と 3px に割れる。余りは中央に置いて余白にする |
| 小数の座標 | 全部整数 |
| 文字が升目に乗らない | ドット書体を**設計サイズの整数倍**でしか使わない（DotGothic16 は 16px 設計 → 仮想 8 ドット） |
| 札の枠を縮小して使い回す | 大きさごとに描き直す。大は中の 2 倍にして、絵は 1 枚で済むようにした |

### にじみ

| | |
|---|---|
| 角丸・ぼかし影・なめらかな階調 | 全部やめて**順序ディザ（4×4 ベイヤー）**で段を作る |
| 斜め線のアンチエイリアス | 回転で作らず**1 ドットずつ階段で刻む**（ガラスの映り込み） |
| 半透明の重ね | 中間色が無限に増えるので、ディザで代用 |

### 色と光

| | |
|---|---|
| 要素ごとに色を選ぶ | 色相ごとの**段（ramp）**で持ち、段からしか選ばない |
| 影がただのグレー | 影は環境の寒色、光は光源の暖色に寄せる |
| 光の向きがばらばら | 全部の凹凸を**左上から**に統一。上と左が 1 ドット明るく、下と右が 1 ドット暗い |
| 奥も手前も同じコントラスト | 奥（上）の石積みはコントラストを落とす |

### ドット絵

| | |
|---|---|
| 平坦な塗り | 形を置いたあと**機械的に陰影を付ける**（空いている側が上か左なら 1 段明るく、下か右なら 1 段暗く） |
| 輪郭が無い／純黒 | **その色相のいちばん暗い段**で縁取る |
| 接地が曖昧 | 人物に硬い接地影、筐体に硬い落ち影。ぼかさない |
| 小さすぎる | 形の座標ごと 1.5 倍にしてから升目に起こす（SVG を引き伸ばすと格子が壊れる） |

### 「貼り付けた感」

| | |
|---|---|
| 盤が「世界に置かれた箱」 | **画面の下半分そのものを筐体の面**にした。盤はその面に開いた窓 |
| 因果の線が無い | 札は筐体の**出口から溝を滑って**並ぶ。溝が 1 本つながっている |
| 取り付けが無い | ガラスの奥に沈め、四隅を**ボルト**で留める |
| 上下が同じ材質 | 世界＝石と木、筐体＝鉄と真鍮。同じ木で作ると床と筐体がつながって段の境が消える |
| 段の境がただの線 | 筐体が床に硬い影を落とす。影が無いと、下段が奥の壁か手前の物か決まらない |
| 操作が世界から浮く | 丸いボタンをやめ、筐体に生えた**縦レバー**にした。右下（親指の来る所）に置く |
| 床が壁の続きに見える | 境に明線を引かず、暗い線 1 本＋ディザで受ける。明線を引くと人物が「棚に乗って」見える |

### 版面

| | |
|---|---|
| 枠の太さが全部同じ | 世界（壁・床・人物）に枠は無く、筐体と HUD にだけ枠がある |
| 数字だけで状態を出す | 記号・数字・色の 3 つで出す（HP・盾・次の一手） |
| 揃えが混ざる | 見出しは左、数字は右 |
| どこを見ればいいか無い | 前の敵に印。次の一手は敵の頭の上にだけ出す |
| 同じ情報が 2 か所にある | 次の一手を敵の頭上とレールの両方に出していた。レールごと廃止 |

## 中身

| | |
|---|---|
| 天井 | `y 0..15`。梁を 96 ドットごと |
| 壁 | `y 0..96`。石積み。見切りを `y 64` に 1 本 |
| 松明 | `x 108` と `x 238`、`y 34`。器具と炎だけ（光の粒は無し） |
| 床 | `y 96..168`。木。足元 `y 138` |
| 右上 | 所持金と持ち物 `y 20..38`、地下何階 `y 42..62` |
| 筐体 | `y 168..360`。鉄と真鍮 |
| 盤の窓 | `x 2..178, y 176..324`。canvas は `x 6, y 180` の 168×140 |
| のこりの投数 | `x 6, y 330`。この手番であと何回投げられるか |
| 投げる | 筐体に生えた縦レバー。`x 192, y 184..303` |
| 手札 | 110×154。中心 `x 436`、間隔は最大 `130`（詰めて `x 236..636` に収める）、下端 `y 344`。出すのは新しい 4 枚まで |
| 実況 | ふだんは `y 172`、拡大中は盤の下の `y 338` |

## 修飾と並び順は廃止した

出た札は**1 枚ずつ独立して効く**。次の札に乗る「修飾」札は無い。
解決の順は**盤で出た順**（玉が収まった順）にしてある。

## 検算

`bshot.js`（枠外に出ていないか・例外）と `bfit.js`（拡大が整数倍か・横あふれ）。

- 640×360 の外に出ている要素なし
- 例外なし
- **ディザが本当に効いている**（12 か所。0 になっていたら上の 1 が再発している）
- 幅 1400 → ×2、1100 → ×1、820 → ×1、430 → 端数（640 より狭いときだけ許す）

### 枠外の検算で 1 回間違えた

`getBoundingClientRect` を素で見ていたので、**ガラスの中の映り込み**（`overflow:hidden`
で切られている斜めの帯）が毎回「枠外」に出ていた。
親の切り取りを掛けてから測るように直した。

ただし **`#scr` 自身の切り取りは掛けてはいけない**。掛けると、本当に画面の下に
はみ出した「投げる」の銘板（`y 365`）まで見逃す。実際に一度見逃した。
切り取りを見てよいのは `#scr` の内側の親だけ。

## ページには画面しか出していない

規格の見本も、素人くささの一覧も、見出しも全部落とした。
画面だけを縦横の中央に置いてある。判断の記録はこの README に残す。

## まだ無いもの

- 属性は無と火だけ。氷・毒の札はまだ 1 枚も無い
- 「力」を付ける札がまだ無い（仕組みだけ入っている）
- 札は 5 枚だけ。買う・捨てる・報酬で拾うは無い（持ち物で直接いじるだけ）
- 玉を増やす「強化」は無い（持ち物から手で増やすだけ）
- 敵は 3 体固定。階が上がっても同じ 3 体が出てくる
- 音は無い。演出も最低限（当たった時の点滅なども無い）
- 数字（HP・ダメージ・階数）は仮。バランスは取っていない
